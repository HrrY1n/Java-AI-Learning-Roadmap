# Java 后端 + AI 应用开发统一学习路线

本路线的短期目标是打牢 **Java 后端基础并完成传统后端项目**，长期目标是具备 Java 业务系统与 Python AI Service 协作开发能力。Java 后端是主技术栈，Python / LLM / RAG / Agent 是第二技术栈；两者最终汇入项目 B“Java 智能知识库平台”。

本文件是完整 **Knowledge Map**，不是每天从上到下执行的课表。现在做什么只看 [学习执行计划](学习执行计划.md)，已经学会什么只看 [真实学习进度](真实学习进度.md)。

## 当前事实

- 当前实际位置：**Stage 01 Java 基础，学习中**。
- Stage 02–03：有课程和参考实现，没有独立完成与验收证据。
- Stage 08：只有早期工程示例；仓库有真实 Git 历史不等于已经掌握 Git。
- Stage 10–12：Python、mock LLM、关键词/TF-IDF RAG 示例仍在，没有被删除；大量 TODO、真实 API、真实 embedding/向量库和完整评测尚未完成。
- Stage 04–07、09、13、14：是尚未学习的路线节点，内容没有丢失；现在只在总路线中描述，进入时才创建教程与练习，避免用空目录制造虚假进度。

## 状态与优先级

状态只由学习证据改变：

- **已完成**：有自己的实现、正常/边界/异常验证和复盘记录，并通过阶段标准。
- **学习中**：有实际练习证据，尚未通过阶段验收。
- **已有材料**：教程、参考实现或生成示例存在，不代表已经学会。
- **未开始**：没有学习证据，或关键内容尚不存在。

优先级按目标岗位分开：

- **P0-J**：当前 Java 后端主线的高价值核心。
- **P0-AI**：进入 AI 应用/RAG 主线或相关项目时的高价值核心。
- **P1**：核心闭环后的重要深化。
- **P2**：当前了解边界，按岗位再学。

优先级只决定学习注意力，不判断求职资格；投递时机由学习者自己决定。

## 主线与并行模型

```text
Java 基础 → OOP → Java Core → MySQL → Spring Boot/MyBatis
→ Redis → 项目 A 稳定 MVP → 项目加固
```

从现在并行但限量推进：算法/面试、CS 快速复习、工程实践、轻量 Python/AI 保温。主线保持 60–70% 关注度；具体当前任务见 [学习执行计划](学习执行计划.md)，能力缺口参考见 [求职能力矩阵](求职能力矩阵.md)。

## 统一 14 Stage 知识地图

| Stage | 能力范围 | 材料/学习状态快照 | 可核验产出 |
| --- | --- | --- | --- |
| [01](stages/stage01_java_basics/README.md) | Java 变量、类型、运算符、条件/循环、数组、方法、Scanner、字符串基础 | 学习中 | 独立控制台程序、数组/双指针题、边界复盘 |
| [02](stages/stage02_java_oop/README.md) | class/对象、构造器、封装、继承、多态、接口/抽象类、String/StringBuilder、对象建模 | 已有材料，未验证 | 一个独立 OOP 小系统及 Review |
| [03](stages/stage03_java_core/README.md) | List/Map/Set、泛型、异常、UTF-8 IO、Lambda/Stream、Repository/Service | 已有材料，练习未完成 | 可持久化学生管理、异常/文件边界和数据结构题 |
| 04 | Java 进阶 + CS 并行层：线程、线程池、锁/JMM；JVM；数据结构算法；HTTP/网络/OS 快速复习 | 未开始 | Stage 03 后随主线穿插并发小实验、JVM/网络排错和算法基线；不阻塞 MySQL |
| 05 | MySQL + 项目 A 数据设计：SQL、建模、索引、事务、锁、EXPLAIN、慢 SQL | 未开始 | 表结构/迁移脚本、代表 SQL、索引与事务实验 |
| 06 | Maven、HTTP/REST、Spring/Boot、MyBatis、分层、校验、异常、日志、事务、认证 | 未开始 | 项目 A 可运行 REST 业务闭环 |
| 07 | Redis 与后端加固：数据结构、TTL、Cache Aside、会话、一致性、热点/故障边界 | 未开始 | 项目 A 一个必要缓存/会话场景与验证 |
| [08](stages/stage08_engineering/README.md) | 持续工程线：Git、命令行/Linux、构建、测试、日志、Docker、部署、CI、观测 | 只有入门示例 | 随主线累积的构建/测试/排错/交付证据 |
| 09 | 项目 A 工程加固（业务从 Stage 05 开始） | 未开始 | 测试/文档/部署/观测/复盘增强；不阻塞 Stage 10 |
| [10](stages/stage10_python_for_ai/README.md) | Python 核心语法 → 工程基础 → FastAPI/数据处理/Java 联调 | 课程存在，未开始 | 可安装、可测试的 Python 工具和 AI Service |
| [11](stages/stage11_llm_apps/README.md) | messages、token/context、structured output、stream、tool、可靠性、成本与基础评测 | 仅 mock 示例 | 一条真实、可测试、可替换模型的调用层 |
| [12](stages/stage12_rag/README.md) | 解析/清洗、chunk/metadata、embedding/vector DB、retrieval/rerank、引用/拒答、更新与评测 | 生成示例存在，未学习 | 真实 RAG Pipeline、回归集、指标和失败分析 |
| 13 | Agent 原理与工程：context、loop/graph、state/checkpoint、memory、HITL、tools、MCP、guardrails、trace | 未开始 | 受控、可恢复、可评测的单 Agent workflow |
| 14 | 项目 B：Java 智能知识库平台 | 未开始 | Java 业务系统 + Python AI Service 联调、部署、评测与项目证据 |

未开始的阶段不创建空目录。进入对应阶段时，才按“场景 → 为什么 → 概念/心智模型 → 最小代码 → 观察/修改 → 独立练习 → 项目复用 → 验证证据”创建有效内容。

## Java、后端与 CS 能力层次

| 领域 | P0-J：当前核心 | P1：后续深化 | P2：按场景选择 |
| --- | --- | --- | --- |
| Java Core | 语法/OOP、String、集合、泛型、异常、IO、Lambda/Stream、Repository/Service 基础 | Optional、注解/反射、NIO、常用设计原则与重构 | 源码级集合细节、响应式生态 |
| 并发 | Thread、Runnable/Callable、`synchronized`、线程池、线程安全与竞态 | Lock、volatile、CAS、ThreadLocal、并发集合、CompletableFuture、JMM | AQS/无锁算法内部实现、高性能调优 |
| JVM | 运行时内存、类加载、GC 基本作用，能读常见 OOM/堆栈 | GC 日志、jstack/jmap/jcmd、内存/线程问题定位 | 收集器深调优、字节码/编译器深入 |
| 算法 | 数组/字符串/哈希/链表/栈队列/树/堆；二分、双指针、滑窗、BFS/DFS、回溯/DP 入门 | 高频题变式、复杂度权衡、项目中数据结构选择 | 竞赛算法、冷门高级结构 |
| MySQL | DDL/DML、CRUD、Join、Group By、子查询、分页、表设计、索引/联合索引、EXPLAIN、ACID/隔离 | B+Tree、MVCC、锁、慢 SQL、索引失效和优化 | 内核、复杂复制、分库分表 |
| Spring 后端 | Maven；JDBC 基线；HTTP/REST/JSON；IoC/DI；Spring MVC/Boot；Controller/Service/Mapper；MyBatis；校验、异常、日志、分页、事务边界、OpenAPI、基础 Security/JWT | AOP、事务失效、自动配置概念、Security 深化、性能与集成测试 | Spring 源码、复杂微服务生态 |
| Redis | String/Hash/List/Set/ZSet、TTL、Cache Aside、会话、穿透/击穿/雪崩基础 | 一致性、热点、持久化、分布式锁边界 | 集群内部实现和大规模调优 |
| 网络/系统 | HTTP 请求响应、method/status/header、REST、Cookie/Session/JWT、CORS；Linux 文件/权限/环境变量、`ps/top`/`kill`、端口、`grep/tail/less`、`curl` | TCP、DNS、TLS/HTTPS、keep-alive、WebSocket、连接池、SSH、`chmod`、`ss/netstat`、`systemctl`/systemd、反向代理与网络抓包 | 协议内核、复杂网络治理 |
| 工程 | Git 分支/冲突、Maven、JUnit、接口测试、日志/debug、环境变量、README/OpenAPI、基础运行与排错 | Docker/Compose、公开部署、Nginx、GitHub Actions、指标/日志/trace | Kubernetes、Service Mesh、大型平台工程 |
| 系统设计 | 从项目解释 API 边界、数据模型、分页、索引、缓存与一致性 | 幂等、限流、超时/重试、异步/MQ、降级、负载均衡、可用性/扩展性/一致性取舍 | 多区域、复杂分片、超大规模分布式架构 |

系统设计不是单独背完再做项目。每加入数据库、缓存、认证、异步或部署，就在项目文档写清需求、约束、方案、失败模式和取舍。

## 数据结构与算法：理解、实现、迁移

算法是一条随 Java 主线并行的思维训练线，不是靠题量解锁下一阶段。每个主题都从具体问题出发，先写可验证的直接方案，再讨论数据规模、时间/空间复杂度、不变量和优化代价。

| 层次 | 核心概念 | 练习方式 |
| --- | --- | --- |
| 分析基础 | 输入规模、Big-O、最好/平均/最坏情况、时间与空间权衡、暴力基线 | 先预测操作次数和边界，再运行小样例验证；解释为什么要优化 |
| 数据结构 | 数组/字符串、链表、栈/队列、Hash/Set、树/BST/堆、图；Trie、并查集、单调结构按真实场景补 | 从访问、查找、插入、删除和顺序要求选择结构，并在 Java 项目代码中再次识别 |
| 解题模式 | 排序、二分、双指针、滑窗、前缀和、递归/分治、BFS/DFS、回溯、贪心、动态规划、Top-K | 写出状态/指针/队列含义和循环不变量；比较直接方案与改进方案，而不是背模板 |

统一动作：**场景与约束 → 选择结构 → 直接方案 → 正常/边界样例 → 复杂度 → 必要时优化 → 改条件观察 → 用自己的话解释 → 在业务代码中复用**。LeetCode 等平台只作为可选题目来源；仓库不设刷题数量、连续打卡或独立追踪系统。

## Python 深度边界：为 AI Service 服务

Python 不与 Java 学到同等深度，也不能省略基础语法。Stage 10 分三段：

| 子阶段 | 必须掌握 | 不扩张到 |
| --- | --- | --- |
| 10A 核心语法 | 变量/字符串、list/tuple/dict/set、流程、函数、module/package、class、typing、异常、pathlib、JSON/CSV | Python 语言高级特性大全 |
| 10B 工程基础 | venv/pip/requirements、环境变量、pytest、logging、项目结构、requests、mock、错误处理 | 再做一套完整 Python Web 后端生态 |
| 10C AI Service | FastAPI 请求/响应 schema、校验、异常、测试、NumPy/Pandas 最小适用边界、Java HTTP 联调 | Django 全栈、数据科学全套理论 |

现有 Stage 10 材料没有删除，但 class/typing、pytest/logging/env、NumPy 和 FastAPI 工程闭环仍需在真正进入阶段时补齐。

## LLM → RAG → Agent 生产知识地图

### LLM 调用层

- messages 协议：system/user/assistant/tool，不把角色历史拼成无法验证的纯文本。
- token/context：估算、截断、摘要、预算与敏感信息边界。
- structured output：schema 校验、修复或有限重试。
- streaming 与可靠性：timeout、retry/backoff、rate limit、cancellation、fallback、幂等边界。
- tool/function calling：参数 schema、权限、副作用、超时与失败结果。
- 模型抽象：业务代码依赖薄接口；记录模型、延迟、token、成本和错误。
- 评测与安全：固定样本、Prompt Injection/敏感信息测试、日志脱敏。

### RAG 层

- 文档：PDF/DOCX/HTML/文本解析、清洗、文档 ID、页码/段落来源、chunk/overlap 和 metadata。
- 索引：真实 embedding、持久向量库、批量/增量更新、版本、删除和权限范围。
- 检索：Top-k、metadata filtering、关键词+向量 hybrid、query rewrite、reranker 与阈值/拒答。
- 生成：上下文预算、引用绑定与验证、低置信拒答、Prompt Injection 防护。
- 评测：Recall@k/MRR 等检索指标、回答相关性/引用正确性、回归测试、失败分类和消融对比。
- 生产：延迟、成本、timeout/retry/rate limit、trace、多租户与文件安全。

### Agent 层

- 原理：context engineering、agent loop、planning、graph/loop、工具选择与终止条件。
- 状态：短/长期 memory 的持久化、检索和生命周期，state、checkpoint、durable execution、resume 与 failure recovery。
- 控制：human-in-the-loop、guardrails、权限、预算、超时/取消、retry/fallback/rate limit、工具失败与幂等。
- 协作：manager/supervisor、agents-as-tools、handoff、specialization；默认单 Agent，只有角色/权限/上下文确实需要分离时才多 Agent。
- 协议：MCP/A2A 的能力与安全边界，不把协议名当业务价值。
- 评测/运维：成功率、轨迹、工具错误、成本、tracing、deployment、security 和回归集。

## Principle > Framework

| 层 | 先掌握的原理 | 默认主实现 | 只作边界比较 |
| --- | --- | --- | --- |
| LLM | messages、schema、stream、可靠性、成本、评测 | 选择一个模型的官方 Python SDK，并包在薄接口后 | 其他模型 SDK、统一网关 |
| RAG | 解析、chunk/metadata、embedding、retrieval/rerank、引用、评测 | 清晰的 Python 模块 + 一个持久向量库；框架可不用 | LangChain 等链式抽象、多个向量库 |
| Agent | loop/state/checkpoint/HITL/tools/guardrails/trace | 确有状态工作流时默认 LangGraph；简单工具调用不用 Agent 框架 | OpenAI Agents SDK 的 manager/handoff 模式、Microsoft 示例生态 |
| Java/AI 桥接 | HTTP 契约、超时、鉴权、幂等、观测 | Java 调用独立 FastAPI AI Service | Spring AI 作为项目成熟后的 P1 Java 侧实验 |

每层只能有一个主实现。替代框架只做小型对比记录，不再复制一条完整 Pipeline。

四层边界保持清楚：**Principle** 是协议、数据流、失败模式和评测，必须先会；**Framework** 是可替换的实现加速器，每层至多主修一个；**Vendor** 是具体模型/API/云服务，只通过薄接口依赖；**Infrastructure** 是数据库、向量库、缓存、队列、容器和观测设施，只有明确需求与运维边界时才引入。项目成果写解决的问题和验证证据，不写工具名称堆砌。

## 项目 A：校园预约与资源管理后端

默认业务用于避免继续犹豫选题；如果将来有更真实的个人需求，可保持验收标准不变地替换业务。

| 版本 | 随 Stage 演进 | 产出 |
| --- | --- | --- |
| A0 | Stage 05 | 用户/角色、资源、时段、预约、取消、审计等需求边界与表设计 |
| A1 | Stage 05 | CRUD、Join、分页、冲突查询、索引与事务实验 |
| A2 | Stage 06 | Spring Boot/MyBatis 分层 REST、校验、统一异常、日志、OpenAPI |
| A3 | Stage 06 | 登录/权限、JWT、预约冲突的事务边界、JUnit 与接口测试 |
| A4 | Stage 07 | Redis 热门资源/可用时段或会话场景；TTL、一致性和故障降级说明 |
| A5 | 稳定 MVP | 可复现启动、Git 历史、Linux 排错记录、项目讲解与真实问题复盘 |
| A6 | Stage 08–09 | Docker/Compose、部署、Nginx/CI/基础观测、性能和系统设计加固 |

A5 表示项目 A 已形成稳定、可解释的核心闭环；A6 在真实需要出现时继续增强。版本号描述项目成熟度，不决定求职时间。项目保持单体优先，不为了简历提前拆微服务。

## 项目 B：Java 智能知识库平台 V0–V12

项目 B 与项目 A 业务目标不同；它证明 Java 与 Python AI Service 的协作，不把同一份 RAG 代码重复包装成第三个项目。

| 版本 | 能力增量 | 验收证据 |
| --- | --- | --- |
| V0 | Java 业务 API 与用户/知识库边界 | 可运行 REST、数据库迁移、测试 |
| V1 | 认证、权限、文件元数据与任务状态 | 权限测试、事务/异常边界 |
| V2 | Redis、日志、OpenAPI、环境配置 | 缓存/会话选择与降级说明 |
| V3 | 文档上传、版本、删除和处理任务 | 文件安全、状态机与重试边界 |
| V4 | Python/FastAPI 服务契约 | `/health`、schema、Java 联调、接口测试 |
| V5 | 真实 LLM 调用层 | messages、structured output、stream、timeout/retry/cancel、成本记录 |
| V6 | 解析、清洗、chunk、metadata、embedding 与持久索引 | 可重复入库、版本/删除和来源锚点 |
| V7 | retrieval、过滤、引用与拒答 | 固定评测集、Recall@k/引用正确性 |
| V8 | hybrid/rerank/rewrite 与回归 | 消融对比、失败分类和改进记录 |
| V9 | 受控 Agent workflow | state/checkpoint、工具 schema、终止条件、HITL（有必要时） |
| V10 | 可靠性、安全与观测 | rate limit、fallback、权限、injection 测试、trace、预算 |
| V11 | Java/Python/MySQL/Redis 联调与部署 | Compose/部署记录、集成测试、故障演练 |
| V12 | 性能、评测、文档与对外证据 | 架构图、真实指标、Git 历史和有证据的问题复盘 |

当前不会提前生成 V0–V12 代码。每一版只在前一版可运行、可测试、可解释后进入。

## 求职与对外表达参考

投递时间、岗位组合和简历版本由学习者自行决定；仓库只帮助识别缺口与核对事实，不设置许可条件。

- **Java 实习**：项目 A 的稳定业务闭环、数据库/API/Redis/测试和排错记录有助于解释真实后端能力。
- **Java 校招/初级岗**：A6 的部署、观测、性能与系统设计能进一步增强项目深度。
- **AI 应用/RAG 岗**：项目 B V5–V8 逐步提供真实 API、持久索引、引用、评测和失败分析证据。
- **Agent/复合岗**：V9–V12 逐步提供可恢复 workflow、评测、可靠性与安全证据；框架名称不能替代这些事实。

简历和面试只陈述自己确实完成并验证的贡献；需求边界、架构、运行方式、测试、Git 历史、故障复盘和真实测量指标可以作为证据。参考实现或 AI 生成代码不得冒充个人成果。

## 学习闭环

1. **Scenario / Why**：从一个真实开发问题和约束出发，说明不用这个概念会发生什么。
2. **Concept / Mental Model**：用少量术语建立输入、状态、数据流和边界的心智模型。
3. **Minimal Code**：运行只包含一个主要概念的参考示例；先预测，再观察输出。
4. **Modify**：只改一个有意义的条件，解释行为、复杂度或失败方式为什么变化。
5. **Exercise**：不看答案独立实现核心逻辑，并覆盖正常、边界、异常或失败依赖。
6. **Real Usage**：在后续练习或项目中再次使用，比较选择与替代方案。
7. **Review / Evidence**：Review 命名、职责、复杂度、安全和可维护性；在 [真实学习进度](真实学习进度.md) 记录自己的路径、命令、结果和复盘。

## 当前明确不投入

- Kubernetes、Service Mesh、复杂微服务治理、复杂 MQ、分库分表和多区域架构。
- AQS/集合/Spring 源码全量深挖、JVM 深度调优和高级并发内部实现。
- 同时学习所有数据库、向量库、模型 SDK 和 Agent 框架。
- 模型训练/微调、CUDA、多模态/GraphRAG；除非目标岗位发生变化。
- 为了显得高级而提前生成项目 B 最终代码、伪造测试/指标或上调学习状态。

## 活动仓库结构

```text
README.md              # 一分钟导航
AGENTS.md              # 项目级协作与验证规则
学习执行计划.md         # 唯一当前执行入口
统一学习路线.md         # 完整知识地图（本文件）
真实学习进度.md         # 真实证据与状态
求职能力矩阵.md         # 岗位能力参考与当前缺口
requirements.txt       # 现有 Python 参考示例依赖
docs/                  # 参考路线审计等少量长期文档
stages/                # 只包含已有有效内容的阶段
archive/               # 忽略的旧脚手架，不属于活动路线
```

旧 Java 课程继续保存在 Stage 01–03 `existing_course/`，生成/参考实现保留在相应 `examples/`。路径均以仓库根目录为基准，不依赖本机绝对路径。

## 验证命令

```powershell
# Python 语法；生成代码能编译不代表已经学会
python -m compileall .

# Java 包示例：进入包含 com/ 的具体课程目录后
javac -encoding UTF-8 -d out com/javaroadmap/stage01/day01outputvariable/App.java
java -cp out com.javaroadmap.stage01.day01outputvariable.App
```
