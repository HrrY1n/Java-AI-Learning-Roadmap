# 历史简历项目草稿（尚未达到使用条件）

> 警告：这是旧路线自动生成的草稿，不是当前已完成项目。只有通过 Stage 12 的真实 embedding、向量数据库、reranker、评测集和 FastAPI 验收，并在 Stage 14 完成 Java 集成后，才能根据自己的真实贡献重写并使用。

## 项目一：基于 RAG 的学术论文与专利文档问答系统

**项目描述：**  
面向学术论文与专利文档阅读场景，使用 Python 实现一个 RAG 文档问答助手。系统支持读取 txt/md 文档，完成文本切分、Top-k 检索、Prompt 拼接、mock LLM 回答生成，并展示引用片段，方便用户复查回答依据。

**技术栈：** Python、文本处理、TF-IDF、Top-k 检索、Prompt、Streamlit、FastAPI、SQLite。

**达到对应验收后，按真实贡献重写的能力维度：**

- 使用 Python 实现文档读取、文本切分和批量处理流程。
- 设计简单向量检索模块，支持根据用户问题返回 Top-k 相关片段。
- 构建 Prompt 模板，将问题与引用片段组合后生成结构化回答。
- 使用 Streamlit 实现上传文档、输入问题、展示回答和引用片段的 Demo。
- 使用 FastAPI 提供 `/health` 和 `/ask` 接口，并预留问答历史保存能力。

## 项目二：RAG 问答质量评测与风险分析工具

**项目描述：**  
为 RAG 文档问答系统设计问答质量评测工具，围绕引用正确性、幻觉检测、错误引用分析和风险样本测试生成 markdown 评测报告，帮助复查回答是否有依据。

**技术栈：** Python、规则评测、Markdown 报告、JSON 数据处理、RAG 评测。

**达到对应验收后，按真实贡献重写的能力维度：**

- 设计引用命中检查逻辑，判断回答是否包含正确引用依据。
- 记录可能存在幻觉风险的问答样本，支持人工复查。
- 自动生成 markdown 评测报告，输出样本结果和风险等级。
- 使用授权测试样本对 RAG 系统进行安全评估和质量分析。
