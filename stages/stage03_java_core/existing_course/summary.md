# Stage03 总结

完成本阶段后，你已经从“数组保存数据”升级到了“集合管理对象”，并初步接触了后端项目常见写法。

你应该掌握：

- `List`、`Map`、`Set` 的基本使用场景。
- 泛型 `Result<T>` 的意义。
- 自定义业务异常。
- 文件读写和 UTF-8 编码。
- Stream 对集合进行筛选和统计。
- Repository 和 Service 的简单分工。

下一阶段会进入 Maven、MySQL 和 JDBC。你会把现在的文件持久化升级为数据库持久化。

