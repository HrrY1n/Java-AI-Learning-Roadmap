# Stage02 练习题

## 学生管理系统

1. 给 `Student` 增加 `email` 字段。
2. 在 `StudentService` 中增加根据 id 查找学生的方法。
3. 增加修改学生分数的方法，并限制分数在 0 到 100。

## 图书管理系统

1. 给 `Book` 增加价格字段。
2. 增加根据书名查找图书的方法。
3. 借书时如果已经借出，要提示不能重复借。

## 银行账户系统

1. 增加账户密码字段。
2. 存钱和取钱都要输出操作后的余额。
3. 增加转账方法，从一个账户转到另一个账户。

