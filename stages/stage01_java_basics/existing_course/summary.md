# Stage01 总结

完成本阶段后，你应该能把 Java 基础语法组合成小程序，而不是只看懂零散语法点。

你应该掌握：

- 使用变量保存数据。
- 使用判断处理不同情况。
- 使用循环重复执行任务。
- 使用数组保存一组数据。
- 使用方法拆分代码。
- 使用 `Scanner` 获取用户输入。
- 使用 `javac` 和 `java` 编译运行程序。

下一阶段会进入面向对象。也就是把现在散落在 `main` 方法中的变量和方法，逐步组织成 `Student`、`Book`、`Account` 这样的类。

