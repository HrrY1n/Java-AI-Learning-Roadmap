# 学习执行计划

这是仓库中唯一回答“现在该学什么”的文件。完整能力范围见 [统一学习路线](统一学习路线.md)，真实状态见 [真实学习进度](真实学习进度.md)。当两者看起来很大时，以本文件的 CURRENT 和 Gate 为准。

## CURRENT

**Stage 01 Java 基础：完成验收，不开后续项目代码。**

1. 重新编译并运行 day01–day06，记录实际命令、结果和错误。
2. 不看答案独立完成成绩统计、计算器和循环菜单；补除数为 0、非法输入、空数组等边界。
3. 完成 6 道数组/循环题，其中至少 2 道双指针入门题。
4. Review `loop.java` 与 `method.java` 的命名、package、职责和边界处理。
5. 在 [真实学习进度](真实学习进度.md) 写下至少 3 个真实错误、修复过程和下一步。

完成标准只看 [Stage 01 README](stages/stage01_java_basics/README.md)，参考答案或已有 `.class` 都不能替代独立实现。

## NEXT

主线一次只推进一个最近里程碑：

1. **Stage 02 OOP**：独立建模并完成一个 OOP 小系统。
2. **Stage 03 Java Core**：集合、泛型、异常、IO、Stream/Lambda；完成 TODO 和数据结构题。
3. **Stage 05 MySQL + 项目 A 数据设计**：业务边界、ER/表结构、SQL、索引和事务实验。
4. **Stage 06 Spring Boot/MyBatis + 项目 A API**：实现可运行的单体 REST 业务闭环。
5. **Stage 07 Redis + 项目 A MVP**：实现一个真实缓存/会话场景，达到 Internship Ready Gate 后开始投递。

Stage 04 中的线程/线程池、JVM、HTTP/网络和算法基础在 Stage 03 之后进入 PARALLEL，不作为去学 MySQL 的整段串行门槛；深度内容继续放 P1。Stage 08 工程能力同样随主线逐步验收。Stage 09 是投递期间对项目 A 的加固，不是开始投递前必须学完的所有内容。

## PARALLEL

下面是方向性配比，不是必须机械凑满的课表。主线受阻时先解决阻塞，不用为了百分比切换任务。

| 轨道 | 当前建议比重 | 当前只做什么 | 何时升级 |
| --- | ---: | --- | --- |
| Java/后端主线 | 60–70% | 当前 Stage 的教程、独立练习、测试、Review | 始终是第一优先级，直到 Internship Gate |
| 算法与面试 | 10–15% | 与当前 Stage 同步的数组、双指针和口头解释 | 逐步加入哈希、链表、栈队列、树、BFS/DFS、二分、滑窗、回溯、DP |
| CS 快速复习 | 约 10% | `javac/java`、进程、端口、HTTP 请求响应等当前问题 | 随 Spring/部署补网络、Linux、OS 与 JVM 排错 |
| 工程能力 | 约 10% | Git 小提交、编译命令、错误记录、最小测试 | 随项目加入 Maven/JUnit、日志、OpenAPI、Docker、CI/CD |
| Python/AI 保温 | 约 5–10% | 主线里程碑完成后，任选一个已有 Python 基础 TODO 或读懂一个 mock 流程 | 达到 Gate 后升为系统 Stage 10 主线；不长期冻结，也不得挤占 Java 主线 |

当前并行任务上限：算法 1 组、CS 1 个概念、工程 1 个可验证动作、AI 0–1 个小练习。做完再领取下一项，避免同时打开多门课程。

## LATER

- 达到 Internship Gate 后：一边投递，一边补强项目 A 测试覆盖、完善文档，并加入 Docker/Compose、部署、CI、并发/JVM/MySQL/Redis 深入和面试复盘。
- 项目 A 有稳定 MVP 后：正式进入 Stage 10，系统补 Python 核心语法、工程基础和 AI Service。
- Stage 10 验收后：真实 LLM API → RAG → 受控 Agent；每一步都先有测试与评测，再加框架。
- 项目 B 按 V0–V12 演进，不一次生成最终平台。

## DO NOT LEARN NOW

- Kubernetes、Service Mesh、复杂微服务治理、分库分表和多区域架构。
- AQS/集合/Spring 源码深挖、JVM 深度调优、复杂并发内部实现。
- 同时学习多个 LLM SDK、向量库或 Agent 框架。
- 模型训练/微调、CUDA、GraphRAG、多模态 RAG 和复杂多 Agent。
- 在没有真实代码、测试和评测前写“已完成项目”或编造指标。

## Internship Ready Gate

以下条件全部能用自己的代码、命令或复盘证明时，进入正式 Java 实习投递；不需要等完 14 个 Stage。

- [ ] **Java Core**：能独立使用 OOP、String、集合、泛型、异常、IO、Stream/Lambda，并解释常见选择和边界。
- [ ] **并发/JVM 基线**：能使用 Thread/Runnable/Callable、`synchronized` 和线程池，能解释内存/类加载/GC 的基本作用；深入排错可在投递期继续。
- [ ] **算法基线**：数组、字符串、哈希、链表、栈队列、树/堆基础，以及二分、双指针、滑窗、BFS/DFS、回溯/DP 入门题能独立完成并讲复杂度。
- [ ] **MySQL**：能设计项目 A 表结构，写 CRUD、Join、Group By、子查询和分页，解释索引/联合索引、EXPLAIN、事务与隔离级别。
- [ ] **Java Web**：项目 A 有 Spring Boot/MyBatis REST 业务闭环，包含参数校验、统一异常、日志、事务边界、分页和基础认证授权。
- [ ] **Redis**：项目 A 至少有一个必要且能解释一致性边界的缓存、TTL 或会话场景，并能说明穿透/击穿/雪崩的基础对策。
- [ ] **工程交付**：有清晰 Git 历史，并完成一次分支开发与安全合并或冲突复盘；有 Maven 构建、JUnit/接口测试、OpenAPI/README、环境变量配置；能在 Linux/PowerShell 查看进程、端口和日志并定位一次真实错误。
- [ ] **项目表达**：能现场启动和修改项目 A，讲清业务边界、数据模型、接口、技术取舍、至少 3 个真实问题及修复；简历只写已验证事实。

Gate 通过后立即开始投递。Docker/Compose、公开部署、Nginx、GitHub Actions 和可观测性会显著增强项目，但其中任何单项都不应成为无限延期投递的理由。

## AI 主线启动条件

- **现在**：只允许轻量 Python/AI 保温；不会改变 Stage 10“未开始”的状态。
- **Stage 10 正式启动**：项目 A 已达到 Internship Gate，Java 主线有稳定可运行版本。
- **Stage 10C 联调**：Java 端已有可调用 REST API；Python 端能通过测试提供 `/health` 和明确的请求/响应 schema。
- **Stage 11**：Python 函数、模块、class、typing、异常、环境变量、pytest、logging、requests/FastAPI 已达到可独立使用。
- **Stage 12**：真实 LLM messages、结构化输出和调用可靠性可测试；Tool Calling 不是基础 RAG 的硬前置。
- **Stage 13**：单次 LLM、工具和 RAG 均有评测后，才引入状态、checkpoint、HITL 或多 Agent。

## 每次学习结束时

1. 记录自己改了什么，而不是只记录“看完”。
2. 写下验证命令及结果，失败也要保留复盘。
3. 更新 [真实学习进度](真实学习进度.md)，只有通过阶段完成标准才上调状态。
4. 下一次仍从本文件 CURRENT 开始，不从完整知识地图随机挑主题。
